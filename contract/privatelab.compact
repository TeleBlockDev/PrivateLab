pragma language_version 0.23;

import CompactStandardLibrary;

witness getHemoglobin(): Uint<16>;

export ledger verified: Boolean;

export circuit verifyHemoglobin(minimum: Uint<16>): [] {
  const hemoglobin = getHemoglobin();
  verified = disclose(hemoglobin >= minimum);
}
